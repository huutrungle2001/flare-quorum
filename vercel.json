{
  "buildCommand": "pnpm --filter @flarequorum/chain-bindings build && pnpm --filter @flarequorum/flare-bindings build && pnpm --filter @flarequorum/tender-room build",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
