# Feasibility spike contracts

Minimal contracts for Gates A–E live here. They compile in the shared contracts
workspace but are not production deployment inputs. Production contracts remain
in the sibling `market`, `receipt`, `safe`, and `test-assets` directories.
