{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm bindings:build && pnpm --filter @flarequorum/flare-bindings build && pnpm --filter @flarequorum/settlement-relay build",
    "watchPatterns": [
      ".nvmrc",
      "apps/relay/**",
      "package.json",
      "packages/chain-bindings/**",
      "pnpm-lock.yaml",
      "pnpm-workspace.yaml",
      "railway.json"
    ]
  },
  "deploy": {
    "startCommand": "node apps/relay/dist/flare-ingress-cli.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ALWAYS",
    "restartPolicyMaxRetries": 10
  }
}
